{
  "mcpServers": {
    "fetch": {
      "command": "pixi",
      "args": ["run", "bun", "x", "--bun", "@modelcontextprotocol/server-fetch"],
      "description": "Fetches online documentation from URL on demand."
    },
    "context7": {
      "command": "pixi",
      "args": ["run", "bun", "x", "--bun", "@upstash/context7-mcp"],
      "description": "Auto-resolves library docs: Go, Rust, Elixir, TS, Flutter, Zig, Mojo, etc."
    },
    "filesystem": {
      "command": "pixi",
      "args": ["run", "bun", "x", "--bun", "@modelcontextprotocol/server-filesystem", "."],
      "description": "Read-only access to repo root only. Cannot escape outside '.'."
    }
  }
}
