// Minimal Next.js config for code-rice project
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Project-specific overrides can be added here
};

module.exports = nextConfig;

