# MASON-generated Buck2 manifest — do not edit by hand.
# Source: infra/build/cue/base.cue — watches base/calc
# Rebuild: buck2 build '//base/calc:check'

genrule(
    name = "check",
    srcs = glob(["lib/**/*.hs", "test/**/*.hs", "calc.cabal"]),
    out = "check.stamp",
    cmd = """
set -euo pipefail
REPO=$(git rev-parse --show-toplevel)
export PATH="$REPO/.pixi/envs/default/bin:/usr/bin:$PATH"
cd "$REPO/base/calc"
cabal build all
touch "$OUT"
""",
    visibility = ["PUBLIC"],
)

