# MASON-generated Buck2 manifest — do not edit by hand.
# Source: infra/build/cue/base.cue — watches base/security
# Rebuild: buck2 build '//base/security:check'

genrule(
    name = "check",
    srcs = glob(["src/**/*.zig", "build.zig.zon", "../build.zig", "../build.zig.zon"]),
    out = "check.stamp",
    cmd = """
set -euo pipefail
REPO=$(git rev-parse --show-toplevel)
export PATH="$REPO/.pixi/envs/default/bin:/usr/bin:$PATH"
cd "$REPO/base"
zig build verify
touch "$OUT"
""",
    visibility = ["PUBLIC"],
)

