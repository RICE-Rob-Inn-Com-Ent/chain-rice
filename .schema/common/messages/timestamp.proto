syntax = "proto3";

package core.common.messages;

// Timestamp is an RFC3339 UTC timestamp with nanosecond precision.
message Timestamp {
  int64 seconds = 1; // Seconds since Unix epoch.
  int32 nanos = 2;   // Non-negative fractions of a second at nanosecond resolution.
}
