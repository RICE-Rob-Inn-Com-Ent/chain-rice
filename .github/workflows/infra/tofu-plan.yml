# KING TODO map
# .github/workflows/infra/tofu-plan.yml — OpenTofu plan (soft-coded)
# CUE mirror for IaC intent: infra/configs/terraform/** (generator → .opentofu/)
# TODO:
# [ ] tofu init/plan per RICE_TOFU_WORKSPACE (dev/stag/prod); backend config from RICE_TOFU_BACKEND_* secrets/vars
# [ ] no plaintext cloud keys — OIDC or RICE_AWS_ROLE_ARN / RICE_GCP_WORKLOAD_ID_PROVIDER style vars only
# [ ] comment plan on PR via RICE_GITHUB_TOKEN with least permissions

name: Infra — OpenTofu plan

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".opentofu/**"
      - ".github/workflows/infra/tofu-plan.yml"

permissions:
  contents: read
  pull-requests: write

jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder (implement tofu plan above)
        run: "true"
