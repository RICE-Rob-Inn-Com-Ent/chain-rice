# KING TODO map
# .github/workflows/ci/build.yml — CUE mirror: infra/configs/github/github__workflows__ci__build_yml.cue
# TODO:
# [ ] replace all workflow_dispatch + push triggers with:
#     branches: [main, 'feat/**', 'fix/**'] — match convco scope branches
# [ ] replace all Bazel build steps with: rice audit (which calls buck2 build //...)
# [ ] add RICE_GITHUB_ORG, RICE_GITHUB_REPO as env vars — read from vars context, never hardcoded
# [ ] add dagger call pipeline step — replaces shell-based CI steps
#     uses: dagger/dagger-action@v1, with: verb: call, args: build
# [ ] add cosign verify step after Docker push
# [ ] add trivy scan step on produced Docker images
# [ ] add buf breaking change check step (proto.yml)
# [ ] use pixi cache: actions/cache key = pixi.lock hash — never reinstall from scratch
# [ ] pin all action versions with digest — renovate pinDigests tracks them
# [ ] add RICE_CI_SLACK_WEBHOOK secret ref for failure notifications
#     RICE_CI_NOTIFY_EMAIL as alternative — soft-coded, never hardcoded address
