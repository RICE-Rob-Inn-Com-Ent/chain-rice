## ChainRice App (Cosmos SDK)

Core blockchain application.

### Commands
```bash
make test       # run Go tests
make install    # install chainrice binary
```

### Notes
- Modules live under `x/chainrice`
- CLI/daemon in `cmd/chainrice`
