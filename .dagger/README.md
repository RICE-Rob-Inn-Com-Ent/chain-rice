# Dagger (KING)

Pipeline CI/CD w root repo. Workflowy BARD (Flutter) przeniesione z `frontend/codemagic.yaml`:

- **bard-flutter-workflows.yaml** — specyfikacja buildów Flutter iOS/macOS (Codemagic); do zaimplementowania w module Dagger.

Od uruchomienia z roota: `.project` = `frontend` (BARD).
