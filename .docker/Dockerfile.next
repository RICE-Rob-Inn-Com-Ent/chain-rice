# TODO:
# [ ] FROM node:22-alpine as runner — minimal Next.js standalone runner
# [ ] COPY buck-out/gen/frontend/browser/.next/standalone /app
# [ ] COPY buck-out/gen/frontend/browser/.next/static /app/.next/static
# [ ] COPY buck-out/gen/frontend/browser/public /app/public
# [ ] ENV NODE_ENV=production
#     NEXT_PUBLIC_API_URL=${RICE_API_URL}
#     NEXT_PUBLIC_WS_URL=${RICE_WS_URL}
#     NEXT_TELEMETRY_DISABLED=1 — no Next.js telemetry ever
#     PORT=${RICE_NEXT_PORT} (default: 3100)
# [ ] USER nextjs — non-root, UID soft-coded via ARG
# [ ] EXPOSE ${RICE_NEXT_PORT}
# [ ] HEALTHCHECK curl -sf http://localhost:${RICE_NEXT_PORT}/api/health
