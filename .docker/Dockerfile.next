# CUE source: infra/configs/docker/Dockerfile.next.cue (package docker; aggregator infra/configs/config.cue)
# KING — Next.js distroless target (build from MASON CUE / `rice pour`).
#
# Final image: gcr.io/distroless/nodejs22-debian12 — requires next.config `output: "standalone"`.
# Build: copy `.next/standalone`, `.next/static`, `public`; CMD ["server.js"]; PORT from env.
# Local compose uses `.docker/Dockerfile.rice-distroless-stub` until artifacts exist.
#
# TODO (implementation):
# [ ] Multi-stage: node:22-bookworm → distroless/nodejs22-debian12
# [ ] NEXT_TELEMETRY_DISABLED=1, non-root UID via COPY --chown
