# CUE source: infra/configs/docker/Dockerfile.otp.cue (package docker; aggregator infra/configs/config.cue)
# KING TODO map
# TODO:
# [ ] FROM erlang:${OTP_VERSION}-slim — OTP release base for Elixir services
# [ ] ARG MIX_ENV=prod — default prod, override to dev via docker-compose profile
# [ ] COPY buck-out/gen/service/otp/_build/${MIX_ENV}/rel /app/releases
#     all Elixir role releases: core, pipeline, cluster, guard, connection in one OTP image
# [ ] ENV RELEASE_NODE=${RICE_OTP_NODE_NAME} — soft-coded node name for distributed Erlang
#     ERLANG_COOKIE=${RICE_ERLANG_COOKIE} — injected via SOPS
#     RELEASE_COOKIE=${RICE_ERLANG_COOKIE}
# [ ] EXPOSE RICE_OTP_HTTP_PORT RICE_OTP_DIST_PORT (4369 EPMD + 9000 dist)
# [ ] HEALTHCHECK ./bin/${RICE_OTP_RELEASE} rpc "System.get_env()" with interval
