# CUE source: infra/configs/docker/docker__bot__Dockerfile.chief.cue (package docker; aggregator infra/configs/config.cue)
# Do not overwrite from CUE gen until shard content is wired; edit the .cue shard.

# KING TODO map
# TODO:
# [ ] FROM debian:bookworm-slim
# [ ] COPY buck-out/gen/base/mint /usr/local/bin/rice-mint — .rice compiler (Rust)
# [ ] COPY buck-out/gen/base/chief /usr/local/bin/rice-chief
# [ ] VOLUME /app/custom — .rice project files mounted from host custom/
# [ ] VOLUME /etc/rice/registry — model registry mounted from .docker/etc/model.json
# [ ] ENV RICE_ROLE=chief
# [ ] ENV RICE_MINT_PATH=/usr/local/bin/rice-mint
# [ ] ENV RICE_CUSTOM_DIR=/app/custom
# [ ] ENV RICE_REGISTRY_PATH=/etc/rice/registry/model.json
# [ ] ENV RICE_CHIEF_MODEL — read from model.json at startup
# [ ] HEALTHCHECK rice-chief --health with RICE_CHIEF_HEALTH_INTERVAL
