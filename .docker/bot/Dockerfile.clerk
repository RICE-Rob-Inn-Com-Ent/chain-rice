# CUE source: infra/configs/docker/docker__bot__Dockerfile.clerk.cue (package docker; aggregator infra/configs/config.cue)
# Do not overwrite from CUE gen until shard content is wired; edit the .cue shard.

# KING TODO map
# TODO:
# [ ] FROM debian:bookworm-slim — needs GHC runtime for Haskell, Zig stdlib
# [ ] COPY buck-out/gen/base/clerk /usr/local/bin/rice-clerk
# [ ] COPY buck-out/gen/base/mint /usr/local/bin/rice-mint — .rice compiler
# [ ] install GHC runtime: libgmp10, libffi8 — Haskell QuickCheck proofs
# [ ] ENV RICE_ROLE=clerk
# [ ] ENV RICE_MINT_PATH=/usr/local/bin/rice-mint
# [ ] ENV RICE_PASETO_KEY_PATH — path to PASETO key file injected via SOPS volume mount
# [ ] EXPOSE RICE_CLERK_PORT (default: 3004)
