# CUE source: infra/configs/docker/bot__Dockerfile.smith.cue (package docker; aggregator infra/configs/config.cue)
# Do not overwrite from CUE gen until shard content is wired; edit the .cue shard.

# KING TODO map
# TODO:
# [ ] FROM gcr.io/distroless/base-debian12 — Go binary, no shell needed
# [ ] COPY buck-out/gen/service/smith /usr/local/bin/rice-smith
# [ ] ENV RICE_ROLE=smith
# [ ] ENV OTEL_SERVICE_NAME=rice-smith
# [ ] ENV OTEL_EXPORTER_OTLP_ENDPOINT — read from RICE_TEMPO_URL at runtime
# [ ] EXPOSE RICE_SMITH_PORT (default: 3000) — ConnectRPC + REST
# [ ] HEALTHCHECK CMD ["/usr/local/bin/rice-smith", "health"]
