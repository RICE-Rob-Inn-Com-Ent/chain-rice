# CUE source: infra/configs/docker/docker__bot__Dockerfile.mason.cue (package docker; aggregator infra/configs/config.cue)
# Do not overwrite from CUE gen until shard content is wired; edit the .cue shard.

# KING TODO map
# TODO:
# [ ] FROM debian:bookworm-slim
# [ ] COPY buck-out/gen/infra/mason /usr/local/bin/rice-mason
# [ ] COPY infra/configs/ /etc/rice/configs/ — CUE templates mounted read-only
# [ ] COPY infra/schemas/ /etc/rice/schemas/ — Proto schemas
# [ ] install runtime deps: cue, buf — versions pinned via ARG, renovate tracks
#     MASON_CUE_VERSION, MASON_BUF_VERSION — injected at build time
# [ ] ENV RICE_ROLE=mason
# [ ] ENV RICE_CONFIGS_DIR=/etc/rice/configs
# [ ] ENV RICE_SCHEMAS_DIR=/etc/rice/schemas
# [ ] VOLUME /etc/rice/configs — MASON writes generated configs here
