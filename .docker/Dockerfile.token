# CUE source: infra/configs/docker/docker__Dockerfile.token.cue (package docker; aggregator infra/configs/config.cue)
# KING TODO map
# TODO:
# [ ] FROM debian:bookworm-slim — Cosmos SDK needs libc
# [ ] COPY buck-out/gen/service/token /usr/local/bin/rice-token
# [ ] ENV RICE_ROLE=token
#     RICE_CHAIN_ID=${RICE_TOKEN_CHAIN_ID} — soft-coded chain identifier
#     RICE_TOKEN_HOME=/data — chain data directory
# [ ] VOLUME /data — chain state, keys, config
# [ ] EXPOSE RICE_COMETBFT_RPC_PORT RICE_COSMOS_REST_PORT RICE_COSMOS_GRPC_PORT
#     RICE_COMETBFT_P2P_PORT — for validator node peering
# [ ] HEALTHCHECK curl -sf http://localhost:${RICE_COSMOS_REST_PORT}/health
