# CUE source: infra/configs/docker/conf__qdrant.conf.cue (package docker; aggregator infra/configs/config.cue)
# Do not overwrite from CUE gen until shard content is wired; edit the .cue shard.

# KING TODO map
# TODO:
# [ ] set storage.storage_path = RICE_QDRANT_STORAGE (default: /qdrant/storage)
# [ ] set storage.snapshots_path = RICE_QDRANT_SNAPSHOTS (default: /qdrant/snapshots)
# [ ] set storage.hnsw_index.m = RICE_QDRANT_HNSW_M (default: 16)
#     HNSW graph connectivity — higher = better recall, more memory
# [ ] set storage.hnsw_index.ef_construct = RICE_QDRANT_EF_CONSTRUCT (default: 100)
# [ ] set storage.quantization.scalar.type = "int8"
#     scalar quantization — 4x memory reduction, ~1% recall loss
#     RICE_QDRANT_QUANTIZATION=none disables for max recall
# [ ] set service.http_port = RICE_QDRANT_HTTP_PORT (default: 6333)
# [ ] set service.grpc_port = RICE_QDRANT_GRPC_PORT (default: 6334)
# [ ] set service.api_key = RICE_QDRANT_API_KEY — read from SOPS at runtime
# [ ] set cluster.enabled = RICE_QDRANT_CLUSTER (default: false)
#     cluster: consensus.tick_period_ms, p2p.port = RICE_QDRANT_P2P_PORT
# [ ] set telemetry_disabled = RICE_QDRANT_TELEMETRY_DISABLED (default: true)
# [ ] configure collections via SAGE on startup — NOT hardcoded here:
#     SAGE creates collections: rice_docs, rice_code, rice_project_{name}
#     vector size read from active embedding model config
