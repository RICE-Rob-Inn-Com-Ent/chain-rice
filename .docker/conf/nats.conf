# Generated by: cue export infra/configs/nats.cue
# Do not edit manually — edit infra/configs/nats.cue instead

# TODO:
# [ ] set server_name = RICE_NATS_SERVER_NAME (default: "rice-nats")
# [ ] set host = "0.0.0.0", port = RICE_NATS_CLIENT_PORT (default: 4222)
# [ ] set http_port = RICE_NATS_MONITOR_PORT (default: 8222) — monitoring endpoint
# [ ] configure JetStream:
#     jetstream { store_dir = RICE_NATS_STORE_DIR (default: /data)
#                 max_memory_store = RICE_NATS_MEM_STORE (default: 1GB)
#                 max_file_store = RICE_NATS_FILE_STORE (default: 10GB) }
# [ ] configure accounts per role — NATS multi-tenancy:
#     accounts: { KING: {users, exports}, MASON: {users}, SMITH: {users, imports, exports},
#                 CLERK: {users}, SAGE: {users, imports}, BARD: {users, imports}, CHIEF: {users} }
#     each account user password read from SOPS secrets
# [ ] configure streams per domain:
#     EVENTS (SMITH publishes, all subscribe), AUDIT (CLERK publishes, immutable)
#     HARDWARE (BARD publishes hw state), COMPUTE (SAGE publishes inference events)
#     COOK (CHIEF publishes compilation events), SECURITY (Zig probe publishes)
#     retention = limits, storage = file, replicas = RICE_NATS_REPLICAS (default: 1)
# [ ] configure max_payload = RICE_NATS_MAX_PAYLOAD (default: 8MB)
# [ ] configure cluster block when RICE_NATS_CLUSTER_ENABLED=true:
#     cluster { name = RICE_NATS_CLUSTER_NAME, port = RICE_NATS_CLUSTER_PORT
#               routes = [RICE_NATS_CLUSTER_ROUTES] }
# [ ] configure TLS block when RICE_NATS_TLS_ENABLED=true:
#     cert_file, key_file read from SOPS secrets
# [ ] configure leafnode block for edge deployments — RICE_NATS_LEAFNODE_ENABLED

# Placeholder NATS configuration.

