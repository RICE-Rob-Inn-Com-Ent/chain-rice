# Generated by: cue export infra/configs/yugabyte.cue
# Do not edit manually — edit infra/configs/yugabyte.cue instead

# TODO:
# [ ] set ysql_enable_auth = true — require auth on all YSQL connections
# [ ] set max_connections = RICE_YB_MAX_CONNECTIONS (default: 300)
# [ ] set ysql_max_connections = RICE_YB_YSQL_MAX_CONNECTIONS (default: 100)
# [ ] set default_transaction_isolation = 'serializable' — CLERK audit log integrity
# [ ] set log_min_duration_statement = RICE_YB_SLOW_QUERY_MS (default: 1000)
#     slow query logging — forwarded to VictoriaMetrics via OTel
# [ ] set track_io_timing = on — I/O stats for SMITH bench/
# [ ] set wal_level = logical — enables logical replication for CLERK audit stream
# [ ] set yb_enable_expression_pushdown = true — pushdown filters to storage layer
# [ ] set yb_index_state_flags_update_delay = 0 — faster index builds in dev
# [ ] set ssl = RICE_YB_SSL_ENABLED (default: off in dev, on in prod)
#     ssl_cert_file and ssl_key_file read from RICE_YB_SSL_CERT_PATH
# [ ] set timezone = 'UTC' — all timestamps UTC
# [ ] set yb_ysql_conn_mgr_enabled = RICE_YB_CONN_MGR (default: false)
#     connection manager — enable when concurrent connections exceed 100

# Placeholder CockroachDB configuration.

