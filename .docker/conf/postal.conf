# CUE source: infra/configs/docker/conf__postal.conf.cue (package docker; aggregator infra/configs/config.cue)
# Do not overwrite from CUE gen until shard content is wired; edit the .cue shard.

# KING TODO map
# TODO:
# [ ] set web.host = RICE_POSTAL_HOST
# [ ] set web.protocol = RICE_POSTAL_PROTOCOL (default: https in prod, http in dev)
# [ ] set main_db.host = RICE_POSTAL_DB_HOST (yugabyte container)
#     main_db.port = RICE_POSTAL_DB_PORT (default: 5433)
#     main_db.username = RICE_POSTAL_DB_USER
#     main_db.password = injected via SOPS
#     main_db.database = RICE_POSTAL_DB_NAME (default: postal)
# [ ] set message_db.host = same as main_db
# [ ] set rails_secret_key = RICE_POSTAL_SECRET_KEY — generated by SOPS on rice pour
# [ ] set smtp_server.port = RICE_POSTAL_SMTP_PORT (default: 25)
#     smtp_server.tls_enabled = RICE_POSTAL_TLS (default: false in dev)
#     smtp_server.tls_cert_path, tls_key_path read from SOPS
# [ ] set dns.mx_records = [RICE_POSTAL_MX_DOMAIN]
#     dns.spf_include = RICE_POSTAL_SPF_DOMAIN
#     dns.return_path_domain = RICE_POSTAL_RETURN_PATH
#     all DNS values soft-coded — filled by MASON on rice pour cloud
# [ ] set signing_key_path = RICE_POSTAL_SIGNING_KEY — DKIM private key via SOPS
