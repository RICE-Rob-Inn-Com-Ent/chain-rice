# CUE source: infra/configs/docker/docker__conf__temporal.conf.cue (package docker; aggregator infra/configs/config.cue)
# Do not overwrite from CUE gen until shard content is wired; edit the .cue shard.

# KING TODO map
# TODO:
# [ ] set persistence.defaultStore = sql
#     sql.pluginName = postgres12
#     sql.databaseName = RICE_TEMPORAL_DB (default: temporal)
#     sql.connectAddr = RICE_TEMPORAL_DB_HOST:RICE_TEMPORAL_DB_PORT
#     sql.user = RICE_TEMPORAL_DB_USER
#     sql.password = injected via SOPS at runtime — never hardcoded
# [ ] set persistence.visibilityStore = sql (same DB, different schema)
# [ ] set global.membership.maxJoinDuration = "30s"
# [ ] set global.metrics.prometheus.listenAddress = ":RICE_TEMPORAL_METRICS_PORT"
#     metrics forwarded to VictoriaMetrics via scrape
# [ ] set log.level = RICE_TEMPORAL_LOG_LEVEL (default: info)
# [ ] set namespaces: [ { name: RICE_TEMPORAL_NAMESPACE, retention: "RICE_TEMPORAL_RETENTION" } ]
#     one namespace per rice project — CHIEF creates namespace on rice prepare
# [ ] set frontend.rpc.grpcPort = RICE_TEMPORAL_GRPC_PORT (default: 7233)
# [ ] set dynamicConfigClient.filepath = /etc/temporal/config/development.yaml
# [ ] add worker.numConcurrentActivityTaskPollers = RICE_TEMPORAL_WORKERS (default: 4)
#     SAGE inference tasks are heavy — limit concurrency to GPU count
