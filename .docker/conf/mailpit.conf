# CUE source: infra/configs/docker/conf__mailpit.conf.cue (package docker; aggregator infra/configs/config.cue)
# Do not overwrite from CUE gen until shard content is wired; edit the .cue shard.

# KING TODO map
# TODO:
# [ ] set MP_SMTP_BIND_ADDR = "0.0.0.0:RICE_MAILPIT_SMTP_PORT" (default: 1025)
# [ ] set MP_UI_BIND_ADDR = "0.0.0.0:RICE_MAILPIT_UI_PORT" (default: 8025)
# [ ] set MP_MAX_MESSAGES = RICE_MAILPIT_MAX_MSG (default: 500) — dev only
# [ ] set MP_DATABASE = RICE_MAILPIT_DB_PATH (default: /data/mailpit.db)
# [ ] set MP_WEBROOT = RICE_MAILPIT_WEBROOT (default: /)
# [ ] enable MP_UI_AUTH_FILE for dev team access control when RICE_MAILPIT_AUTH=true
# [ ] dev profile only — this service NEVER starts in production
#     enforced by docker-compose.yml profiles: [dev]
