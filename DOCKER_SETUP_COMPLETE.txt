╔══════════════════════════════════════════════════════════════════════╗
║                                                                      ║
║  ✅  COMPLETE DOCKER SETUP - ALL SERVICES  ✅                        ║
║                                                                      ║
╚══════════════════════════════════════════════════════════════════════╝

🎯 WSZYSTKO W JEDNYM docker-compose.yml!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📦 ŁĄCZNIE: 21 SERWISÓW

✅ FRONTEND (2 serwisy)
   • UI Kit (Vite)          → Port 5173   → ui.rice.local
   • Next.js Web            → Port 3001   → app.rice.local

✅ BACKEND CORE (6 serwisów)
   • MongoDB                → Port 27017
   • PostgreSQL             → Port 5432
   • Data Service           → Port 8080
   • GraphQL Gateway        → Port 4000   → api.rice.local
   • Bot Core               → Port 8100
   • Bot Integrations       → Port 8200

✅ GODS - AI SERVICES (6 bogów)
   • ⚱️ Thoth (Text)         → 8001, 11434 → thoth.rice.local
   • ☀️ Ra (Graphics)        → 8002        → ra.rice.local
   • ✨ Isis (Medical)       → 8003        → isis.rice.local
   • 🐱 Bastet (Vision)      → 8004        → bastet.rice.local
   • ⚖️ Maat (Legal)         → 8005, 11438 → maat.rice.local
   • 💰 Khnum (Finance)      → 8006, 11439 → khnum.rice.local

✅ MONITORING STACK (7 serwisów)
   • Traefik (Proxy)        → 80, 443, 8080
   • Prometheus (Metrics)   → 9090
   • Loki (Logs)            → 3100
   • Grafana (Dashboards)   → 3000        → grafana.rice.local
   • Elasticsearch          → 9200, 9300
   • Logstash               → 5000, 9600
   • Jaeger (Tracing)       → 16686       → jaeger.rice.local

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 INTEGRACJE:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Dockerfiles:
   • .frontend/web/Dockerfile       (Vite UI Kit)
   • .project/web/Dockerfile        (Next.js Web)
   • .backend/bot/core/gods/*/Dockerfile (6 Gods)

✅ Konfiguracje (.devcontainer/config/):
   • grafana/provisioning/datasources/datasources.yml
   • prometheus/prometheus.yml
   • loki/loki-config.yml
   • traefik/traefik.yml
   • logstash/logstash.yml + pipeline/

✅ Networks:
   • rice-frontend   (Frontend services)
   • rice-backend    (Backend services)
   • pantheon        (Gods + Monitoring)

✅ Volumes (20 wolumenów):
   Databases:     mongo-data, postgres-data
   Gods Models:   thoth-models, ra-models, isis-models, bastet-models, 
                  maat-models, khnum-models
   LoRA:          thoth-lora, ra-lora, isis-lora, bastet-lora,
                  maat-lora, khnum-lora
   Monitoring:    prometheus-data, loki-data, grafana-data, 
                  elasticsearch-data
   Cache:         bazel-cache, model-cache

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 JAK URUCHOMIĆ:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. PODSTAWOWE (Backend Core + Frontend):
   docker-compose up -d

2. GODS (AI Services):
   docker-compose --profile gods up -d

3. MONITORING:
   docker-compose --profile monitoring up -d

4. WSZYSTKO NARAZ:
   docker-compose --profile all up -d

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOKUMENTACJA:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   📄 DOCKER_COMPLETE_GUIDE.md    ← Kompletny przewodnik
   📄 docker-compose.yml          ← Główny plik konfiguracji
   📁 .devcontainer/config/       ← Wszystkie konfiguracje

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎨 PROFILES:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   (default)  → Backend Core + Frontend (9 serwisów)
   gods       → Tylko Gods AI Services (6 serwisów)
   monitoring → Monitoring Stack (7 serwisów)
   all        → WSZYSTKO! (21 serwisów)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✨ Everything is ready! All services in one docker-compose.yml! ✨

