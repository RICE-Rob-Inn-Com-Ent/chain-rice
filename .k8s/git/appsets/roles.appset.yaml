# KING TODO map
# .k8s/git/appsets/roles.appset.yaml — role workloads ApplicationSet (soft-coded)
# CUE mirror: infra/configs/kubernetes/git__appsets__roles.appset.yaml.cue
# TODO:
# [ ] One Application per rice role (king, sage, smith, …); image tags from RICE_IMAGE_TAG / RICE_REGISTRY
# [ ] Sync waves / dependencies for CHIEF → BARD ordering if required

apiVersion: v1
kind: ConfigMap
metadata:
  name: king-stub-appset-roles
  namespace: default
  annotations:
    rice.king/source-cue: infra/configs/kubernetes/git__appsets__roles.appset.yaml.cue
data: {}
