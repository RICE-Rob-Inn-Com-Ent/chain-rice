# KING TODO map
# .k8s/git/appsets/data.appset.yaml — data plane ApplicationSet (soft-coded)
# CUE mirror: infra/configs/kubernetes/git__appsets__data.appset.yaml.cue
# TODO:
# [ ] Scope NATS/Qdrant/Yugabyte/Ray apps; chart versions from RICE_*_CHART_VERSION env vars
# [ ] Namespace pattern rice-data or RICE_K8S_NAMESPACE_DATA
# [ ] Values files pulled from SOPS/ExternalSecrets refs — no plaintext credentials

apiVersion: v1
kind: ConfigMap
metadata:
  name: king-stub-appset-data
  namespace: default
  annotations:
    rice.king/source-cue: infra/configs/kubernetes/git__appsets__data.appset.yaml.cue
data: {}
