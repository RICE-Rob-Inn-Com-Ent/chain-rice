# KING TODO map
# .k8s/git/apps/root.yaml — root Argo CD Application (soft-coded)
# CUE mirror: infra/configs/kubernetes/k8s__git__apps__root_yaml.cue
# TODO:
# [ ] kind: Application; metadata.name from RICE_ARGOCD_ROOT_APP_NAME
# [ ] spec.source.repoURL: ${RICE_GITHUB_REPO_URL}, targetRevision: ${RICE_K8S_TARGET_REV}
# [ ] spec.destination: server from RICE_K8S_API_URL, namespace from RICE_ARGOCD_ROOT_NAMESPACE
# [ ] syncPolicy automated prune/selfHeal; no hardcoded cluster endpoints

apiVersion: v1
kind: ConfigMap
metadata:
  name: king-stub-git-apps-root
  namespace: default
  annotations:
    rice.king/source-cue: infra/configs/kubernetes/k8s__git__apps__root_yaml.cue
data: {}
