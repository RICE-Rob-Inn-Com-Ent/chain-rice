# Code generated by MASON — cue cmd genK8s. DO NOT EDIT.
apiVersion: v1
kind: ConfigMap
metadata:
  name: mason-stub-install
  namespace: obs
  labels:
    app.kubernetes.io/part-of: tempo
    infra.mason/source-cue: infra/k8s/cue/stack/generate.cue
    infra.mason/stack-file: install.yaml
    infra.mason/chief-profile: prod
data:
  TODO.md: Tempo — traces; params.obs.tempo.enabled.
  values-local.yaml: |-
    profile: prod
    replicaCount: 1
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        memory: 1Gi
  values-prod.yaml: |-
    profile: prod
    replicaCount: 3
    podDisruptionBudget:
      minAvailable: 1
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        memory: 1Gi
  HELM.md: |-
    # tempo — Helm install

    Active CHIEF profile: prod. Use values-prod.yaml when profile is local or prod.

    ```bash
    helm upgrade --install tempo <CHART> \
      --namespace obs --create-namespace \
      -f values-prod.yaml
    ```
    See .k8s/stack/helm-catalog/helm-catalog.yaml for chart coordinates.
