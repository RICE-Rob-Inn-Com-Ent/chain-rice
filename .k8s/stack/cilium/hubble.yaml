# Code generated by MASON — cue cmd genK8s. DO NOT EDIT.
apiVersion: v1
kind: ConfigMap
metadata:
  name: mason-stub-hubble
  namespace: kube-system
  labels:
    app.kubernetes.io/part-of: cilium
    infra.mason/source-cue: infra/k8s/cue/stack/generate.cue
    infra.mason/stack-file: hubble.yaml
    infra.mason/chief-profile: local
data:
  TODO.md: Cilium CNI — tunnel/LB/hubble from params + profile values.
  values-local.yaml: |-
    profile: local
    replicaCount: 1
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        memory: 1Gi
  values-prod.yaml: |-
    profile: prod
    replicaCount: 3
    podDisruptionBudget:
      minAvailable: 1
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        memory: 1Gi
  HELM.md: |-
    # cilium — Helm install

    Active CHIEF profile: local. Use values-local.yaml when profile is local or prod.

    ```bash
    helm upgrade --install cilium <CHART> \
      --namespace kube-system --create-namespace \
      -f values-local.yaml
    ```
    See .k8s/stack/helm-catalog/helm-catalog.yaml for chart coordinates.
