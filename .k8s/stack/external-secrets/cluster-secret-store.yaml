# Code generated by MASON — cue cmd genK8s. DO NOT EDIT.
apiVersion: v1
kind: ConfigMap
metadata:
  name: mason-stub-cluster-secret-store
  namespace: external-secrets
  labels:
    app.kubernetes.io/part-of: external-secrets
    infra.mason/source-cue: infra/k8s/cue/stack/generate.cue
    infra.mason/stack-file: cluster-secret-store.yaml
    infra.mason/chief-profile: local
data:
  TODO.md: External Secrets — ClusterSecretStore scaffold.
  values-local.yaml: |-
    profile: local
    replicaCount: 1
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        memory: 1Gi
  values-prod.yaml: |-
    profile: prod
    replicaCount: 3
    podDisruptionBudget:
      minAvailable: 1
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        memory: 1Gi
  HELM.md: |-
    # external-secrets — Helm install

    Active CHIEF profile: local. Use values-local.yaml when profile is local or prod.

    ```bash
    helm upgrade --install external-secrets <CHART> \
      --namespace external-secrets --create-namespace \
      -f values-local.yaml
    ```
    See .k8s/stack/helm-catalog/helm-catalog.yaml for chart coordinates.
