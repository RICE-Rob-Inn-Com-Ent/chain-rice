# Code generated by MASON — cue cmd genK8s. DO NOT EDIT.
apiVersion: v1
kind: ConfigMap
metadata:
  name: mason-stub-install
  namespace: velero
  labels:
    app.kubernetes.io/part-of: velero
    infra.mason/source-cue: infra/k8s/cue/stack/generate.cue
    infra.mason/stack-file: install.yaml
    infra.mason/chief-profile: prod
data:
  TODO.md: Velero — cluster backup/restore.
  values-local.yaml: |-
    profile: prod
    replicaCount: 1
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        memory: 1Gi
  values-prod.yaml: |-
    profile: prod
    replicaCount: 3
    podDisruptionBudget:
      minAvailable: 1
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        memory: 1Gi
  HELM.md: |-
    # velero — Helm install

    Active CHIEF profile: prod. Use values-prod.yaml when profile is local or prod.

    ```bash
    helm upgrade --install velero <CHART> \
      --namespace velero --create-namespace \
      -f values-prod.yaml
    ```
    See .k8s/stack/helm-catalog/helm-catalog.yaml for chart coordinates.
