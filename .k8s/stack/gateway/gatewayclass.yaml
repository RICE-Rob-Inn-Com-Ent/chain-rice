# Code generated by MASON — cue cmd genK8s. DO NOT EDIT.
apiVersion: v1
kind: ConfigMap
metadata:
  name: mason-stub-gatewayclass
  namespace: gateway
  labels:
    app.kubernetes.io/part-of: gateway
    infra.mason/source-cue: infra/k8s/cue/stack/generate.cue
    infra.mason/stack-file: gatewayclass.yaml
    infra.mason/chief-profile: prod
data:
  TODO.md: Gateway API — ingress controller slice.
  values-local.yaml: |-
    profile: prod
    replicaCount: 1
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        memory: 1Gi
  values-prod.yaml: |-
    profile: prod
    replicaCount: 3
    podDisruptionBudget:
      minAvailable: 1
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        memory: 1Gi
  HELM.md: |-
    # gateway — Helm install

    Active CHIEF profile: prod. Use values-prod.yaml when profile is local or prod.

    ```bash
    helm upgrade --install gateway <CHART> \
      --namespace gateway --create-namespace \
      -f values-prod.yaml
    ```
    See .k8s/stack/helm-catalog/helm-catalog.yaml for chart coordinates.
