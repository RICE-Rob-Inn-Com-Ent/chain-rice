#!/usr/bin/env bash
set -euo pipefail

APP=examples_elixir_hello
ROOT=$(cd "$(dirname "$0")/.." && pwd)

if [ ! -d "$ROOT/_build/prod/rel/$APP" ]; then
  (cd "$ROOT" && MIX_ENV=prod mix release $APP)
fi

exec "$ROOT/_build/prod/rel/$APP/bin/$APP" start


