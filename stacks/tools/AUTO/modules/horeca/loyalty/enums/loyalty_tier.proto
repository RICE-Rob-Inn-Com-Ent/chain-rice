syntax = "proto3";

package horeca.loyalty.enums;

// Loyalty program tiers
enum LoyaltyTier {
  LOYALTY_TIER_UNSPECIFIED = 0;
  LOYALTY_TIER_BRONZE = 1;                // Entry level tier
  LOYALTY_TIER_SILVER = 2;                // Mid-level tier
  LOYALTY_TIER_GOLD = 3;                  // High-level tier
  LOYALTY_TIER_PLATINUM = 4;              // Premium tier
  LOYALTY_TIER_DIAMOND = 5;               // Highest tier
  LOYALTY_TIER_VIP = 6;                   // VIP tier
}
