## Architektura

- `build.gradle.kts` / `settings.gradle.kts`: konfiguracja Gradle (KTS).
- `src/main/kotlin/App.kt`: punkt wejścia (`main`) z funkcją `greet`.
- `Utils.kt`: drobne narzędzia (np. `upcase`).

### Flow

- `./gradlew run --args "Ala"`

### Rozszerzanie

- Dodaj testy (JUnit 5), moduły/dalsze pakiety, integrację z Ktor/Spring.


