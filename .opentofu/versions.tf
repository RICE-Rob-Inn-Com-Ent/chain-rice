# KING TODO map
# .opentofu/versions.tf — soft-coded
# TODO:
# [ ] terraform required_version = ">= RICE_TF_MIN_VERSION" — renovate tracks
# [ ] required_providers: aws, google, azurerm — version vars
# [ ] required_providers: kubernetes, helm, kubectl, sops
# [ ] backend empty here — environments/*/backend.tf

