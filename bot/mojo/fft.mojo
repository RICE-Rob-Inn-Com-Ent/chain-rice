"""
FFT and convolution kernels (from data/compute.py).
Entry points: fft, fftfreq, convolve, filter_signal.
"""

fn convolve_scalar(a: Float64, v: Float64) -> Float64:
    """Convolution placeholder (single value). Extend to 1D arrays for signal pipeline."""
    return a * v
