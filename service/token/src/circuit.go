package token

// CircuitIntegration documents wiring for cosmossdk.io/x/circuit (circuit breaker, disabled Msg URLs).
// Import: circuitkeeper "cosmossdk.io/x/circuit/keeper" — pin cosmossdk.io/x/circuit to the same cosmos-sdk generation as in go.mod.
type CircuitIntegration struct{}
