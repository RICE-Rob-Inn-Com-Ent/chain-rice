name: infinir_desktop
description: InfiniR Desktop - imports from .frontend/flutter/
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Import components from monorepo frontend
  flutter_frontend:
    path: ../../.frontend/flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
